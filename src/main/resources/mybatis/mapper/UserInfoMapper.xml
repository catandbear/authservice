<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.catandbear.data.mapper">
	
	<select id="selectUserByName" resultType="com.catandbear.data.UserInfo">
	    select * from user_info where user_name = #{userName}
	</select>
	
	<insert id="addUser" parameterType="com.catandbear.data.UserInfo">
	    insert into user_info (user_name,password,user_type, email) values (#{userName},#{passWord},#{userType}, #{email})
	</insert>
	
	<update id="updateUser" parameterType="com.catandbear.data.UserInfo">
	    update user_info set name=#{name},pwd=#{pwd} where id = #{id}
	</update>
	
	<delete id="deleteUser" parameterType="com.catandbear.data.UserInfo">
	    delete from user_info where id = #{id}
	</delete>
</mapper>